// Package api hosts the versioned REST handlers, middleware chain
// (request-id, auth, RBAC, rate limit, security headers), and the
// WebSocket tail endpoint. Handlers are thin; see docs/api.md.
package api
